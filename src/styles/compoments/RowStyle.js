import { StyleSheet } from 'react-native';

const RowStyle = StyleSheet.create({
    style1: {
        flexDirection: 'row',
        alignItems: 'center',
        width: '100%',
        marginBottom: 10
    },
});

export default RowStyle;